export class Slider {

    public url: string;
    public desc: string;
    public redirect: string;
    constructor(
        url: string,
        desc: string,
        redirect: string
    ) { this.url = url;
        this.desc = desc;
        this.redirect = redirect;
    }
}


